

## Hide Lovable Badge

Add a CSS rule to `src/index.css` to globally hide the element with the ID `lovable-badge`:

```css
#lovable-badge {
  display: none !important;
}
```

This will be added at the end of the file.

